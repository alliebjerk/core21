{
  "mcpServers": {
    "notion": {
      "command": "npx",
      "args": [
        "-y",
        "@anthropic-ai/mcp-server-notion"
      ],
      "env": {
        "NOTION_API_KEY": "${NOTION_API_KEY}"
      }
    },
    "google-drive": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-google-drive"
      ],
      "env": {
        "GDRIVE_CLIENT_ID": "${GMAIL_CLIENT_ID}",
        "GDRIVE_CLIENT_SECRET": "${GMAIL_CLIENT_SECRET}"
      }
    }
  }
}